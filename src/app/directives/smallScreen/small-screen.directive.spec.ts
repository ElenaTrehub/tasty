import { SmallScreenDirective } from './small-screen.directive';

describe('SmallScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new SmallScreenDirective();
    expect(directive).toBeTruthy();
  });
});
