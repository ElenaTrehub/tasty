import { MiddleScreenDirective } from './middle-screen.directive';

describe('MiddleScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new MiddleScreenDirective();
    expect(directive).toBeTruthy();
  });
});
