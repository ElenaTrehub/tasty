import { LargeScreenDirective } from './large-screen.directive';

describe('LargeScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new LargeScreenDirective();
    expect(directive).toBeTruthy();
  });
});
